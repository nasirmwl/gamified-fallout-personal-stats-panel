# Character stat panel

This repo is **boilerplate / a starting point** for a small static “stats” UI and daily checklist. The copy, percentages, labels, and placeholder image are **examples only** — they **do not describe or reflect any real person included me**, including the author.

Use it as a template: swap text, numbers, `avatar.svg`, and checklist items to match what you actually track.
